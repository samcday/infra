apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
secretGenerator:
  - name: lab-butane
    files:
      - base.yaml
      - install.yaml
      - node.yaml
      - worker.yaml
    options:
      disableNameSuffixHash: true

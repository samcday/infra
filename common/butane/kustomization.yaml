apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
secretGenerator:
  - name: butane-common
    files:
      - fucksecurity.yaml
      - install.yaml
      - kubeadm-worker.yaml
      - tailscale.yaml
    options:
      disableNameSuffixHash: true

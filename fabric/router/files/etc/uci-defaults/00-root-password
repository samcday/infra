set -uexo pipefail

# A random, intentionally unknown password prevents an empty-password login.
# Administration uses SSH public keys; set a human-held recovery password over
# a wired SSH session after the first boot if LuCI access is required.
echo 'root:$6$tSAMy5G9CVGkxzoY$Uxojac5CGqgKN/WIxwDuwHxZHbITR8dUB0dBoVnNSKbOlynOhbLqp8bYfHp56.OemUP3zNSJdE6gEh1sTTD3R.' | /usr/sbin/chpasswd -e

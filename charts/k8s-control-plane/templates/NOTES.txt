{{- if (empty (default $.Values.externalIP $.Values.externalHostname)) -}}
{{- fail "either .externalHostname or .externalIP must be set" -}}
{{- end -}}
{{- if (empty (default "" $.Values.clusterName)) -}}
{{- fail "missing .clusterName" -}}
{{- end -}}

A control-plane for the cluster "{{ $.Values.clusterName }}" has been deployed. It can be reached at https://{{- default $.Values.externalIP $.Values.externalHostname -}}:6443/

{{- if (empty (default (list) $.Values.serviceCIDRs)) -}}
{{- fail "missing serviceCIDRs" -}}
{{- end -}}
{{- if (gt 2 (len $.Values)) -}}
{{- fail "too many serviceCIDRs" -}}
{{- end -}}

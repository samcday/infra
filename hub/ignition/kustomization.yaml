apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
secretGenerator:
  - name: butane
    files:
      - base.yaml=base.bu
      - control-plane.yaml=control-plane.bu
      - etcd.yaml=etcd.bu
      - tpm.yaml=tpm.bu
      - var.yaml=var.bu
    options:
      disableNameSuffixHash: true

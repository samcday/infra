apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: fastboopmos-namespace
  namespace: cloud-cluster
spec:
  chart:
    spec:
      chart: charts/resources
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: infra
        namespace: flux-system
  driftDetection:
    mode: enabled
  interval: 1h
  kubeConfig:
    secretRef:
      name: admin-kubeconfig
  storageNamespace: kube-system
  targetNamespace: kube-system
  values:
    _:
      kind: Namespace
    fastboopmos:
    fastboopmos-reconciler-role:
      apiVersion: rbac.authorization.k8s.io/v1
      kind: Role
      metadata:
        name: fastboopmos-reconciler
        namespace: fastboopmos
      rules:
        - apiGroups: ["*"]
          resources: ["*"]
          verbs: ["*"]
    fastboopmos-reconciler-rb:
      apiVersion: rbac.authorization.k8s.io/v1
      kind: RoleBinding
      metadata:
        name: fastboopmos-reconciler
        namespace: fastboopmos
      roleRef:
        apiGroup: rbac.authorization.k8s.io
        kind: Role
        name: fastboopmos-reconciler
      subjects:
        - kind: ServiceAccount
          name: fastboopmos-reconciler
          namespace: cloud-cluster

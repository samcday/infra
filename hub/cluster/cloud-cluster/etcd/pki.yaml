apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: etcd-peer
spec:
  commonName: etcd
  dnsNames:
    - etcd
    - etcd.etcd.svc
    - etcd.etcd.svc.cluster.local
    - '*.etcd.etcd.svc'
    - '*.etcd.etcd.svc.cluster.local'
  issuerRef:
    kind: ClusterIssuer
    name: etcd
  privateKey:
    algorithm: ECDSA
  secretName: etcd-peer
  usages:
    - digital signature
    - key encipherment
    - client auth
    - server auth
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: etcd-root
spec:
  commonName: root
  issuerRef:
    kind: ClusterIssuer
    name: etcd
  privateKey:
    algorithm: ECDSA
  secretName: etcd-root
  usages:
    - digital signature
    - key encipherment
    - client auth

apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: wasmcloud-demo
  namespace: binarylane-demo
spec:
  chart:
    spec:
      chart: charts/resources
      reconcileStrategy: Revision
      sourceRef:
        kind: GitRepository
        name: infra
        namespace: flux-system
  dependsOn:
    - name: wasmcloud
  driftDetection:
    mode: enabled
  interval: 1h
  kubeConfig:
    secretRef:
      name: admin-kubeconfig
  storageNamespace: wasmcloud
  targetNamespace: wasmcloud
  values:
    hello-world:
      apiVersion: runtime.wasmcloud.dev/v1alpha1
      kind: WorkloadDeployment
      spec:
        replicas: 6
        template:
          spec:
            hostSelector:
              hostgroup: default
            components:
              - name: hello-world
                image: ghcr.io/wasmcloud/components/hello-world:0.1.0
            hostInterfaces:
              - namespace: wasi
                package: http
                interfaces:
                  - incoming-handler
                config:
                  host: wasmcloud-demo.binarylane-demo.samcday.com
    route:
      apiVersion: gateway.networking.k8s.io/v1
      kind: HTTPRoute
      spec:
        hostnames:
          - wasmcloud-demo.binarylane-demo.samcday.com
        parentRefs:
          - name: shared
            namespace: kube-system
        rules:
          - matches:
              - path:
                  type: PathPrefix
                  value: /
            backendRefs:
              - name: runtime-gateway
                port: 80

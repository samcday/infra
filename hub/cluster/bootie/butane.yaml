apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: butane
  namespace: bootie
spec:
  decryption:
    provider: sops
    secretRef:
      name: ignition-key
  interval: 1h
  path: hub/ignition
  prune: true
  sourceRef:
    kind: GitRepository
    name: infra
    namespace: flux-system
  targetNamespace: bootie
  timeout: 5m

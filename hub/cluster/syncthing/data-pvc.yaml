apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: syncthing
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 1000Gi
  storageClassName: ceph-filesystem-ec

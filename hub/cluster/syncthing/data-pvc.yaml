apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: syncthing
spec:
  accessModes: [ReadWriteMany]
  resources:
    requests:
      storage: 500Gi
  storageClassName: ceph-filesystem-ec

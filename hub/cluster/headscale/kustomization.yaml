apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: headscale
configMapGenerator:
  - name: config
    files:
      - acls.json
      - config.yaml
resources:
  - ../../../.kustomize-config
  - helm-release.yaml
  - hub-apiserver-tailnet.yaml
  - noise-privkey.yaml
  - tofu.yaml
  - vpas.yaml

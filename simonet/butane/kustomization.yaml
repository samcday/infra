apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
secretGenerator:
  - name: butane
    files:
      - base.yaml
      - install.yaml
      - worker.yaml
    options:
      disableNameSuffixHash: true

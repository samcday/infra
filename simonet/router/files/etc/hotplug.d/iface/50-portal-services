[ "wan" = "$INTERFACE" ] || exit 0

[ ifup = "$ACTION" ] && svc_action=start
[ ifdown = "$ACTION" ] && svc_action=stop


[ -n "$svc_action" ] && {
    service frr "$svc_action"
    service tang "$svc_action"
}
